# Provisions /run/ryzenadj/ (0777, tmpfs) at boot so both the user's
# ryzenadj wrapper and root-run hooks (sleep/AC) can share its lock,
# cache, and pending-call state files without permission errors.
d /run/ryzenadj 0777 root root -
