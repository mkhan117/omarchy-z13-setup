# See https://wiki.hyprland.org/Configuring/Monitors/
# List current monitors and resolutions possible: hyprctl monitors
# Format: monitor = [port], resolution, position, scale

# Generic Z13 default: named eDP-1 (required for iio-hyprland auto-rotation
# and Omarchy's scaling cycle) at the panel's preferred mode, 2x scale.
# Replace this with an explicit mode line (see the commented examples below)
# if `hyprctl monitors` shows you need one.
monitor=eDP-1,preferred,auto,2
exec-once = iio-hyprland

# Optimized for retina-class 2x displays, like 13" 2.8K, 27" 5K, 32" 6K.
env = GDK_SCALE,2
# monitor=eDP-1,2880x1800@180.00,0x0,2.00,bitdepth,10,vrr,1

# Example fixed-mode + external monitor setup (adjust to your own panel and
# any external display before uncommenting — these modes are author-specific
# from the upstream z13flow dotfiles, not generic Z13 defaults):
# monitor=eDP-1,2560x1600@180.00,384x1152,2.00,bitdepth,10,cm,srgb
# monitor=DP-1,2560x1440@239.96,0x0,1.25,bitdepth,10,cm,srgb  # cm,srgb prevents auto-HDR on fullscreen

# Single
#monitor=eDP-2,2560x1600@180.00,0x0,2.00,bitdepth,10

# External
#monitor=eDP-1,disable
#monitor=DP-1,2560x1440@239.96,0x0,1.25



# ========== HDR Configuration ==========
# Enable experimental color management protocol for HDR support
# experimental {
#     xx_color_management_v4 = true
# }

# # Enable fullscreen HDR passthrough
# # render {
# #     cm_fs_passthrough = 1
# # }

# # # Samsung OLED with HDR10 support (Monitor v2 syntax)
# monitorv2 {
#     output = eDP-1
#     mode = 2880x1800@120
#     position = 0x0
#     scale = 2
#     bitdepth = 10
#     cm = hdr
#     supports_wide_color = 1
#     supports_hdr = 1
#     sdrbrightness = 1.15
#     sdrsaturation = 1.6
# }

# ========== Other Monitor Examples (Commented) ==========
# monitor=,preferred,auto,auto
# monitor = eDP-1, 2880x1800@60, auto, 2

# Good compromise for 27" or 32" 4K monitors (but fractional!)
# env = GDK_SCALE,1.75
# monitor=,preferred,auto,1.666667

# Straight 1x setup for low-resolution displays like 1080p or 1440p
# env = GDK_SCALE,1
# monitor=,preferred,auto,1

# Example for Framework 13 w/ 6K XDR Apple display
# monitor = DP-5, 6016x3384@60, auto, 2
# monitor = eDP-1, 2880x1920@120, auto, 2
